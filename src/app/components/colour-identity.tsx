import ColourIdentityTool from "./colour-identity-tool";
import SectionBody from "./section-body";

const ColourIdentity: React.FC = () => (
  <SectionBody title="Colour Identity Tool">
    <ColourIdentityTool />
  </SectionBody>
);

export default ColourIdentity;
